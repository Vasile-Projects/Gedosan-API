package it.vszdev.gedosanapi.repositories;

import it.vszdev.gedosanapi.models.Trasfusionale;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TrasfusionaleRepository extends JpaRepository<Trasfusionale, Long> {
}
