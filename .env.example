# Variabili d'ambiente per il profilo di PRODUZIONE (SPRING_PROFILES_ACTIVE=prod).

# --- Database ---
DB_USERNAME=
DB_PASSWORD=

# --- Mail (SMTP) ---
MAIL_USERNAME=
MAIL_PASSWORD=

# --- JWT (stringa base64, >= 32 byte) ---
JWT_SECRET=


CORS_ALLOWED_ORIGINS=
